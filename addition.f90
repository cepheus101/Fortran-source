program addition
        implicit none
        integer :: a,b,sum
        print *, 'Enter value of a='
        read(*,*) a
        print *, 'Enter value of b='
        read(*,*) b
        sum=a+b
        print *, 'Sum=', sum
end program addition        
